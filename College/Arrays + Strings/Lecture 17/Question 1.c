#include<stdio.h>
int main()
{
    char str[20] ;
    printf("Enter the String :");
    scanf("%s",&str); // or may use gets(str);
    printf("Entered string is : %s",str); // or may use puts(str);
    return 0 ;
}
